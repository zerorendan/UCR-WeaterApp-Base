package cr.ac.ucr.weather.core.utils.constant

object Database {
    const val FORECAST_TABLE = "forecast_data"
    const val DATABASE_NAME  = "weather_data.db"
    const val CITY_TABLE = "city_data"
    const val READ_ALL_FORECAST = "SELECT * FROM forecast_data"
    const val READ_ALL_CITY = "SELECT * FROM city_data"
}